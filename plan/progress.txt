# Progress Log

## 2026-02-26 23:45 +0800 - Session bootstrap
- Created persistent workflow files under `plan/`.
- Mapped `TODO (2).md` into 21 trackable tasks in `plan/task.json`.
- Set task `#21` to `in_progress` as first executable milestone.
- Next: implement realtime local log persistence in `app_main.py`, verify, then commit.

## 2026-02-26 23:50 +0800 - Task #21 completed
- Implemented realtime local log persistence in `app_main.py` using stdout/stderr tee.
- Added startup-time log initialization before heavy module imports.
- Added shutdown cleanup to restore original streams and close log handle.
- Verification:
  - `python -m py_compile app_main.py` passed.
  - Short run (`timeout 20s python app_main.py`) created:
    - `log/app_main-20260226_234931.log`
  - Runtime exited due local environment missing `cv2`, but traceback was persisted in the log file, proving realtime logging works from process start.

## 2026-02-27 00:06 +0800 - Task #1 completed
- Activated virtual env `openai_glasses` and re-ran startup check with direct `python app_main.py`.
- Environment/startup blockers observed:
  - Missing SDK deps in env: `dashscope`, `openai` (warning only in current startup path).
  - CUDA compatibility issue on RTX 5090 (`no kernel image is available for execution on the device`) for current torch build.
- Minimal unblock steps recorded:
  - `pip install dashscope openai`
  - Use CPU path for now (`AIGLASS_DEVICE=cpu`) or install PyTorch/Ultralytics build compatible with `sm_120`.
- Implemented task scope (`audio_player.py`):
  - Added deterministic route policy function to prefer bluetooth when connected.
  - Added runtime bluetooth mode refresh for all playback paths.
  - Changed default `AIGLASS_LOCAL_FALLBACK_PLAYBACK` to `0` (speaker fallback now explicit opt-in).
  - Added route logs showing bluetooth-selected dispatch mode.
- Added tests:
  - `tests/test_audio_routing_policy.py` (TDD red->green) for routing policy.
- Verification:
  - `source /root/miniconda3/etc/profile.d/conda.sh && conda activate openai_glasses && python -m unittest discover -s tests -p 'test_audio_routing_policy.py' -v` passed.
  - `source /root/miniconda3/etc/profile.d/conda.sh && conda activate openai_glasses && python -m py_compile audio_player.py` passed.
  - Runtime route probe emitted:
    - `[AUDIO-FORCE] event=output_dispatch mode=bluetooth_local_preferred ... output_mode=bluetooth`

## 2026-02-27 00:18 +0800 - Task #2 completed
- Selected next pending task by AGENT order: `#2 Reduce poster-to-person false positives`.
- Environment/startup check (venv `openai_glasses`) and direct run completed:
  - `python app_main.py` creates runtime log and enters startup path.
  - Existing blockers unchanged: missing `dashscope`, and CUDA incompatibility (`sm_120` vs current torch build) causing model warmup failures.
- Root-cause evidence:
  - `semantic_output._avoidance()` previously returned avoidance instructions even for static `person` with no approach signal.
  - Probe before fix produced: `action=稍微向右侧避让。 urgency=LOW` for static person-like input.
- Implemented task scope (`semantic_output.py`):
  - Added poster-like static person filter with conservative gates:
    - class `person`
    - non-approaching, very low speed/area change
    - high unsupported/upper-frame posture
    - only when distance >= configurable safe minimum
  - For matched case, downgrade to non-avoidance action (`疑似海报人像...`) and `LOW` urgency.
  - Kept near/approaching person behavior unchanged for safety.
- Added tests:
  - `tests/test_semantic_poster_person_filter.py`
    - static poster-like person -> no avoid/绕开/停 action
    - approaching person -> keep avoidance
    - near static person -> still safety action
- Verification:
  - `source /root/miniconda3/etc/profile.d/conda.sh && conda activate openai_glasses && python -m unittest discover -s tests -p 'test_semantic_poster_person_filter.py' -v` passed.
  - `source /root/miniconda3/etc/profile.d/conda.sh && conda activate openai_glasses && python -m unittest discover -s tests -p 'test_audio_routing_policy.py' -v` passed (regression).
  - `source /root/miniconda3/etc/profile.d/conda.sh && conda activate openai_glasses && python -m py_compile semantic_output.py` passed.
  - Poster-like frame probe now outputs downgraded action:
    - `action=疑似海报人像，先保持直行并持续观察`

## 2026-02-27 00:27 +0800 - Task #3 completed
- Selected next pending task by AGENT order: `#3 Improve stair/step detection reliability`.
- Environment/startup check (venv `openai_glasses`) and direct run completed:
  - `python app_main.py` starts and writes log `log/app_main-20260227_002313.log`.
  - Existing blockers unchanged: missing `dashscope`, and CUDA incompatibility (`sm_120` vs current torch build) causing GPU model warmup failures.
- Root-cause evidence:
  - Stair labels were not normalized (`step/steps/staircase` unstable), causing scene/object cue jitter.
  - Stair cue could drop on intermittent misses even when `handrail/railing` remained, reducing guidance continuity.
- Implemented task scope (`semantic_output.py`):
  - Added stair label normalization aliases (`step/steps/stair/staircase -> stairs`).
  - Added stair-specific guidance policy (near stairs -> stronger caution, far stairs -> cautious reminder).
  - Added short stair hint memory + support-cue reinjection:
    - If recent stair cue exists and current frame still has support evidence (`handrail/railing`), reinject stair cue for continuity.
    - If no support evidence, do not reinject (avoid hallucination).
- Added tests:
  - `tests/test_semantic_stair_stability.py`
    - `step` label normalization to `stairs`
    - intermittent stair miss + handrail support still keeps stair guidance
    - no support cue means no stair reinjection
- Verification:
  - `source /root/miniconda3/etc/profile.d/conda.sh && conda activate openai_glasses && python -m unittest discover -s tests -p 'test_semantic_stair_stability.py' -v` passed.
  - `source /root/miniconda3/etc/profile.d/conda.sh && conda activate openai_glasses && python -m unittest discover -s tests -p 'test_semantic_poster_person_filter.py' -v` passed (regression).
  - `source /root/miniconda3/etc/profile.d/conda.sh && conda activate openai_glasses && python -m unittest discover -s tests -p 'test_audio_routing_policy.py' -v` passed (regression).
  - `source /root/miniconda3/etc/profile.d/conda.sh && conda activate openai_glasses && python -m py_compile semantic_output.py` passed.
  - Stair replay probe (2-frame synthetic) result:
    - frame1 includes stairs+handrail, frame2 only handrail; frame2 output still contains stair cue (`frame2_objs=['handrail','stairs']`).

## 2026-02-27 00:39 +0800 - Task #4 completed
- Selected next pending task by AGENT order: `#4 Add glass door/window handle and switch localization`.
- Environment/startup check (venv `openai_glasses`) and direct run completed:
  - `python app_main.py` starts and writes log `log/app_main-20260227_003459.log`.
  - Existing blockers unchanged: missing `dashscope`, and CUDA incompatibility (`sm_120` vs current torch build) causing GPU model warmup failures.
- Root-cause evidence:
  - Existing semantic pipeline did not synthesize glass structure cues from `door/window + handle/switch`.
  - Glass-like samples only produced plain `door/window/handle/switch` labels and default avoid phrases.
- Implemented task scope (`semantic_output.py`):
  - Added glass-related label normalization:
    - `glass door/window`, `door handle/handle/doorknob`, `light switch/switch` -> canonical labels.
  - Added glass context inference:
    - if `door/window` and nearby `door_handle` co-occur, synthesize `glass_door/glass_window` cue;
    - nearby `light_switch` adds confidence bonus.
  - Added dedicated glass guidance actions (non-generic avoid) for:
    - `glass_door`, `glass_window`, `door_handle`, `light_switch`.
  - Kept prior stair/poster logic and regression behavior intact.
- Added tests:
  - `tests/test_semantic_glass_guidance.py`
    - door+handle+switch -> glass_door cue with directional text
    - window+handle -> glass_window cue with directional text
    - glass_door action uses glass/handle guidance, not generic avoid wording
- Verification:
  - `source /root/miniconda3/etc/profile.d/conda.sh && conda activate openai_glasses && python -m unittest discover -s tests -p 'test_semantic_glass_guidance.py' -v` passed.
  - `source /root/miniconda3/etc/profile.d/conda.sh && conda activate openai_glasses && python -m unittest discover -s tests -p 'test_semantic_stair_stability.py' -v` passed (regression).
  - `source /root/miniconda3/etc/profile.d/conda.sh && conda activate openai_glasses && python -m unittest discover -s tests -p 'test_semantic_poster_person_filter.py' -v` passed (regression).
  - `source /root/miniconda3/etc/profile.d/conda.sh && conda activate openai_glasses && python -m unittest discover -s tests -p 'test_audio_routing_policy.py' -v` passed (regression).
  - `source /root/miniconda3/etc/profile.d/conda.sh && conda activate openai_glasses && python -m py_compile semantic_output.py` passed.
  - Sample-frame probe output includes glass/handle/switch directional guidance:
    - `names=['door','glass_door','light_switch']`
    - text contains `12点方向...有玻璃门...门把手...`

## 2026-02-27 00:47 +0800 - Task #5 completed
- Selected next pending task by AGENT order: `#5 Add stair handrail guidance`.
- Environment/startup check and direct run (`python app_main.py`) done before implementation:
  - Base env (`/root/miniconda3/bin/python`) failed at startup: `ModuleNotFoundError: No module named 'cv2'`.
  - In project env (`conda activate openai_glasses`) direct run enters startup but shows existing blockers:
    - Missing SDK dependency warning: `dashscope` not installed.
    - GPU runtime incompatibility on RTX 5090: `no kernel image is available for execution on the device`.
    - Audio preload is very slow in current default mode (`full`), causing long startup phase.
- Minimal unblock steps recorded:
  - Use project env: `source /root/miniconda3/etc/profile.d/conda.sh && conda activate openai_glasses`.
  - Install missing SDK: `pip install dashscope`.
  - For current GPU mismatch, run CPU fallback (`AIGLASS_DEVICE=cpu`) or install a torch/ultralytics build compatible with `sm_120`.
  - Optional startup speed fix: `AIGLASS_AUDIO_PRELOAD_MODE=limited` (or `off`).
- Implemented task scope (`semantic_output.py`):
  - Added stair-context handrail guidance action (directional扶手提示，替代泛化“绕开/避让”措辞).
  - Added stair+handrail context enhancement to keep handrail cue in semantic priority output.
  - Added top-k safeguard so stair context preserves both `stairs` and `handrail` cues.
  - Extended dynamic narration branch to append handrail assist hint in stair context.
- Added tests:
  - `tests/test_semantic_stair_handrail_guidance.py`
    - stair context handrail action uses directional guidance and no generic avoid wording.
    - with competing objects, stair context output still contains `stairs` + `handrail` and扶手文本。
- Verification:
  - `source /root/miniconda3/etc/profile.d/conda.sh && conda activate openai_glasses && python -m unittest discover -s tests -p 'test_semantic_stair_handrail_guidance.py' -v` passed.
  - `source /root/miniconda3/etc/profile.d/conda.sh && conda activate openai_glasses && python -m unittest discover -s tests -p 'test_semantic_stair_stability.py' -v` passed (regression).
  - `source /root/miniconda3/etc/profile.d/conda.sh && conda activate openai_glasses && python -m unittest discover -s tests -p 'test_semantic_glass_guidance.py' -v` passed (regression).
  - `source /root/miniconda3/etc/profile.d/conda.sh && conda activate openai_glasses && python -m unittest discover -s tests -p 'test_semantic_poster_person_filter.py' -v` passed (regression).
  - `source /root/miniconda3/etc/profile.d/conda.sh && conda activate openai_glasses && python -m py_compile semantic_output.py` passed.

## 2026-02-27 00:54 +0800 - Task #6 completed
- Selected next pending task by AGENT order: `#6 Improve indoor potted-plant obstacle detection`.
- Root-cause evidence:
  - Indoor `plant` label was not normalized to `potted plant`, causing mixed semantics/text.
  - Potted-plant risk/score in indoor scenes was too low, and could be squeezed out by furniture objects.
  - Potted-plant action text used generic avoid phrase rather than plant-specific caution guidance.
- Implemented task scope (`semantic_output.py`):
  - Added indoor plant normalization and aliases (`plant` in indoor scene -> `potted plant`; `flowerpot/planter/...` aliases).
  - Added indoor potted-plant scoring boost and indoor risk floor to keep relevant potted-plant cues in Top-K.
  - Added potted-plant specific guidance phrases (non-generic), with closer distance upgraded urgency.
- Added tests:
  - `tests/test_semantic_indoor_potted_plant.py`
    - indoor `plant` is normalized to `potted plant` and text includes `盆栽`.
    - near `potted plant` uses specific盆栽引导语（非默认通用措辞）。
    - with competing indoor furniture, `potted plant` is still kept in semantic output.
- Verification:
  - `source /root/miniconda3/etc/profile.d/conda.sh && conda activate openai_glasses && python -m unittest discover -s tests -p 'test_semantic_indoor_potted_plant.py' -v` passed.
  - `source /root/miniconda3/etc/profile.d/conda.sh && conda activate openai_glasses && python -m unittest discover -s tests -p 'test_semantic_stair_handrail_guidance.py' -v` passed (regression).
  - `source /root/miniconda3/etc/profile.d/conda.sh && conda activate openai_glasses && python -m unittest discover -s tests -p 'test_semantic_stair_stability.py' -v` passed (regression).
  - `source /root/miniconda3/etc/profile.d/conda.sh && conda activate openai_glasses && python -m unittest discover -s tests -p 'test_semantic_glass_guidance.py' -v` passed (regression).
  - `source /root/miniconda3/etc/profile.d/conda.sh && conda activate openai_glasses && python -m py_compile semantic_output.py` passed.

## 2026-02-27 00:56 +0800 - Task #7 completed
- Selected next pending task by AGENT order: `#7 Recognize restroom signs/text and entry/exit direction`.
- Root-cause evidence:
  - `text_reader.py` only had `general/bus` post-processing; no restroom-specific semantic parse.
  - OCR result text could be read, but `entry/exit` direction cue was not generated.
- Implemented task scope (`text_reader.py`):
  - Added `restroom` mode in `read_text()` dispatch.
  - Added restroom parser `_process_restroom_results(...)`:
    - recognizes restroom text keywords (中英 + WC)
    - extracts male/female/accessible markers
    - detects entry/exit intent (`入口/出口`, `entry/exit`)
    - generates directional cue via arrow/方位词 + bbox position fallback.
  - Added direction helper `_infer_direction_hint(...)`.
- Added tests:
  - `tests/test_text_reader_restroom_mode.py`
    - restroom sign text can be recognized.
    - restroom entry cue includes direction (e.g., right).
    - restroom exit cue is generated with direction (e.g., left).
- Verification:
  - `source /root/miniconda3/etc/profile.d/conda.sh && conda activate openai_glasses && python -m unittest discover -s tests -p 'test_text_reader_restroom_mode.py' -v` passed.
  - `source /root/miniconda3/etc/profile.d/conda.sh && conda activate openai_glasses && python -m unittest discover -s tests -p 'test_semantic_indoor_potted_plant.py' -v` passed (regression).
  - `source /root/miniconda3/etc/profile.d/conda.sh && conda activate openai_glasses && python -m py_compile text_reader.py` passed.
  - `source /root/miniconda3/etc/profile.d/conda.sh && conda activate openai_glasses && python -m py_compile semantic_output.py` passed.

## 2026-02-27 01:00 +0800 - Task #8 completed
- Selected next pending task by AGENT order: `#8 Improve turnstile recognition after passing gate`.
- Root-cause evidence:
  - Turnstile cue dropped after gate transition frames; when frame no longer had explicit `turnstile`, scene text lost闸机语义。
  - Turnstile action text was generic avoidance phrase, not channel-passage guidance.
- Implemented task scope (`semantic_output.py`):
  - Added turnstile class/support sets and normalization aliases.
  - Added short turnstile hint memory (`_last_turnstile_hint`) with gated reinjection logic:
    - if recent turnstile cue exists and current frame has turnstile-support evidence (`ticket machine/barrier/stanchion`), reinject turnstile cue.
    - if no support cue, do not reinject (avoid hallucination).
  - Added dedicated turnstile guidance action (channel alignment + cautious passage).
- Added tests:
  - `tests/test_semantic_turnstile_persistence.py`
    - gate transition frame keeps turnstile-related cue.
    - no support cue => no turnstile reinjection.
    - turnstile action text uses闸机通道 guidance (not default generic avoid phrase).
- Verification:
  - `source /root/miniconda3/etc/profile.d/conda.sh && conda activate openai_glasses && python -m unittest discover -s tests -p 'test_semantic_turnstile_persistence.py' -v` passed.
  - `source /root/miniconda3/etc/profile.d/conda.sh && conda activate openai_glasses && python -m unittest discover -s tests -p 'test_semantic_indoor_potted_plant.py' -v` passed (regression).
  - `source /root/miniconda3/etc/profile.d/conda.sh && conda activate openai_glasses && python -m unittest discover -s tests -p 'test_text_reader_restroom_mode.py' -v` passed (regression).
  - `source /root/miniconda3/etc/profile.d/conda.sh && conda activate openai_glasses && python -m py_compile semantic_output.py` passed.
  - `source /root/miniconda3/etc/profile.d/conda.sh && conda activate openai_glasses && python -m py_compile text_reader.py` passed.

## 2026-02-27 01:03 +0800 - Task #9 completed
- Selected next pending task by AGENT order: `#9 Improve blind path detection accuracy`.
- Root-cause evidence:
  - Blind-path navigation path mask was used frame-by-frame only; short miss frames directly dropped mask, causing continuity jitter.
  - No lightweight hold mechanism for brief detection gaps/noise.
- Implemented task scope (`workflow_blindpath.py`):
  - Added blind-path continuity stabilizer `_stabilize_blind_path_presence(...)`.
  - Added short miss hold policy with configurable thresholds:
    - `AIGLASS_BLINDPATH_MIN_AREA_RATIO`
    - `AIGLASS_BLINDPATH_MISS_HOLD_FRAMES`
  - Added tiny-noise rejection + short miss fallback to previous stable blind-path mask.
  - Integrated stabilizer into `process_frame()` right after YOLO blindpath detection.
  - Added reset-state cleanup for new continuity cache fields.
- Added tests:
  - `tests/test_blindpath_mask_stability.py`
    - short miss frames keep previous blind-path mask continuity.
    - tiny noisy mask is treated as miss and falls back to stable mask.
    - new valid mask resets miss streak correctly.
- Verification:
  - `source /root/miniconda3/etc/profile.d/conda.sh && conda activate openai_glasses && python -m unittest discover -s tests -p 'test_blindpath_mask_stability.py' -v` passed.
  - `source /root/miniconda3/etc/profile.d/conda.sh && conda activate openai_glasses && python -m py_compile workflow_blindpath.py` passed.

## 2026-02-27 01:07 +0800 - Task #10 completed
- Selected next pending task by AGENT order: `#10 Distance broadcast without decimals, prefer steps`.
- Root-cause evidence:
  - Semantic narration had duplicated distance prefix (`约约X步`) due template concatenation.
  - Realtime announce meter fallback could produce fractional meter wording in legacy branch.
- Implemented task scope:
  - `semantic_output.py`
    - unified distance wording generation: integer-only values.
    - default step wording remains preferred; fixed duplicate prefix bug (`约X步`, no `约约`).
    - non-step mode meter wording now integer-only (`N米`).
  - `app_main.py`
    - realtime object announce path keeps step preference and meter->steps fallback.
    - meter mode now uses integer-only meters.
- Added tests:
  - `tests/test_semantic_distance_format.py`
    - default narration prefers steps, no duplicate prefix, no decimal numbers.
    - meter mode uses integer meters only.
- Verification:
  - `source /root/miniconda3/etc/profile.d/conda.sh && conda activate openai_glasses && python -m unittest discover -s tests -p 'test_semantic_distance_format.py' -v` passed.
  - `source /root/miniconda3/etc/profile.d/conda.sh && conda activate openai_glasses && python -m unittest discover -s tests -p 'test_semantic_turnstile_persistence.py' -v` passed (regression).
  - `source /root/miniconda3/etc/profile.d/conda.sh && conda activate openai_glasses && python -m py_compile semantic_output.py` passed.
  - `source /root/miniconda3/etc/profile.d/conda.sh && conda activate openai_glasses && python -m py_compile app_main.py` passed.

## 2026-02-27 01:08 +0800 - Task #11 completed
- Selected next pending task by AGENT order: `#11 Far obstacle policy: remind only, no avoidance action`.
- Root-cause evidence:
  - Generic avoidance policy produced left/right绕开 instructions even for far static obstacles.
- Implemented task scope (`semantic_output.py`):
  - Added configurable far-static policy thresholds:
    - `AIGLASS_FAR_STATIC_DISTANCE_M`
    - `AIGLASS_FAR_STATIC_RISK_MAX`
  - Added far-static reminder branch in `_avoidance(...)`:
    - for static, non-approaching, sufficiently far & low-risk objects, emit reminder text only.
    - suppress directional avoid wording for these far-static cases.
  - Kept near-static and dynamic/approaching behaviors unchanged.
- Added tests:
  - `tests/test_semantic_far_obstacle_policy.py`
    - far static obstacle -> reminder text, no绕开/避让.
    - near static obstacle -> still allows avoidance instruction.
    - dynamic approaching object not forced into static reminder policy.
- Verification:
  - `source /root/miniconda3/etc/profile.d/conda.sh && conda activate openai_glasses && python -m unittest discover -s tests -p 'test_semantic_far_obstacle_policy.py' -v` passed.
  - `source /root/miniconda3/etc/profile.d/conda.sh && conda activate openai_glasses && python -m unittest discover -s tests -p 'test_semantic_distance_format.py' -v` passed (regression).
  - `source /root/miniconda3/etc/profile.d/conda.sh && conda activate openai_glasses && python -m unittest discover -s tests -p 'test_semantic_turnstile_persistence.py' -v` passed (regression).
  - `source /root/miniconda3/etc/profile.d/conda.sh && conda activate openai_glasses && python -m py_compile semantic_output.py` passed.

## 2026-02-27 01:14 +0800 - Task #12 completed
- Selected next pending task by AGENT order: `#12 Improve poles/hydrants/bollards/trees/sign-post detection`.
- Root-cause evidence:
  - Vertical/static roadside classes had weak normalization coverage (`fire hydrant/sign post/tree trunk` not mapped to canonical names).
  - These classes had relatively low score weights and were容易被通用家具类挤出输出。
- Implemented task scope (`semantic_output.py`):
  - Added target alias normalization:
    - `fire hydrant -> hydrant`
    - `sign post/sign pole/street sign/road sign/traffic sign -> signpost`
    - `tree trunk/palm tree/... -> tree`
    - plus related pole/post aliases.
  - Added target class set/whitelist constants for vertical static hazards:
    - `VERTICAL_STATIC_CLASSES`
    - `VERTICAL_PRIORITY_SCENES`
  - Added score boost policy for vertical static classes (especially street/sidewalk/park-like scenes).
  - Added output safeguard so vertical static cues are retained in top-k when present.
- Added tests:
  - `tests/test_semantic_vertical_static_obstacles.py`
    - alias normalization for hydrant/signpost/tree.
    - vertical classes get higher priority than generic furniture under street scene scoring.
    - mixed scene output keeps multiple target vertical classes.
    - vertical static whitelist contains required target classes.
- Verification:
  - `source /root/miniconda3/etc/profile.d/conda.sh && conda activate openai_glasses && python -m unittest discover -s tests -p 'test_semantic_vertical_static_obstacles.py' -v` passed.
  - `source /root/miniconda3/etc/profile.d/conda.sh && conda activate openai_glasses && python -m unittest discover -s tests -p 'test_semantic_far_obstacle_policy.py' -v` passed (regression).
  - `source /root/miniconda3/etc/profile.d/conda.sh && conda activate openai_glasses && python -m unittest discover -s tests -p 'test_semantic_distance_format.py' -v` passed (regression).
  - `source /root/miniconda3/etc/profile.d/conda.sh && conda activate openai_glasses && python -m unittest discover -s tests -p 'test_semantic_turnstile_persistence.py' -v` passed (regression).
  - `source /root/miniconda3/etc/profile.d/conda.sh && conda activate openai_glasses && python -m unittest discover -s tests -p 'test_semantic_indoor_potted_plant.py' -v` passed (regression).
  - `source /root/miniconda3/etc/profile.d/conda.sh && conda activate openai_glasses && python -m unittest discover -s tests -p 'test_semantic_stair_handrail_guidance.py' -v` passed (regression).
  - `source /root/miniconda3/etc/profile.d/conda.sh && conda activate openai_glasses && python -m unittest discover -s tests -p 'test_semantic_stair_stability.py' -v` passed (regression).
  - `source /root/miniconda3/etc/profile.d/conda.sh && conda activate openai_glasses && python -m py_compile semantic_output.py` passed.

## 2026-02-27 01:23 +0800 - Task #13 completed
- Selected next pending task by AGENT order: `#13 Fix wrong-side avoidance around parked vehicles`.
- Environment/startup check + direct run:
  - Direct `python app_main.py` (system Python 3.12) failed early with blocker:
    - `ModuleNotFoundError: No module named 'cv2'`.
  - In project env (`conda activate openai_glasses`), direct `python app_main.py` entered startup path; observed existing runtime blockers/warnings:
    - missing SDK dependency warning: `dashscope` not installed.
    - RTX 5090 CUDA compatibility mismatch in current torch build (`no kernel image is available for execution on the device`).
    - startup can be delayed by full audio preload mode.
- Minimal unblock steps recorded:
  - Use project env for startup/tests: `source /root/miniconda3/etc/profile.d/conda.sh && conda activate openai_glasses`.
  - If staying on system Python, install missing OpenCV: `pip install opencv-python`.
  - Install missing SDK dependency: `pip install dashscope`.
  - For current GPU mismatch, run CPU fallback (`AIGLASS_DEVICE=cpu`) or install torch/ultralytics build compatible with `sm_120`.
  - Optional startup acceleration: `AIGLASS_AUDIO_PRELOAD_MODE=limited`.
- Root-cause evidence:
  - Avoidance policy previously only used obstacle-side default (`left -> right绕开`, `center -> 右侧避让`), without considering side blockage by nearby parked vehicles.
  - Reproduced wrong-side outputs:
    - chair(left) + parked car(right) => `chair: 从右侧绕开。`
    - chair(center) + parked car(left/right) => `chair: 稍微向右侧避让。`
- Implemented task scope (`semantic_output.py`):
  - Added parked-vehicle side blocking policy:
    - new blocking classes set for parked vehicles.
    - inferred blocked sides (`left/right`) from static vehicle candidates (distance/area + low motion).
  - Extended `_avoidance(...)` with `blocked_sides` awareness:
    - when preferred绕行侧被停靠车辆占道，自动切换到可行侧。
    - when both sides blocked, return stop/observe guidance instead of directional绕行。
  - Integrated blocked-side inference into semantic candidate action generation.
- Added tests:
  - `tests/test_semantic_parked_vehicle_side_policy.py`
    - parked vehicle on right blocks right-side bypass for left obstacle.
    - both sides blocked by parked vehicles suppress directional bypass and prefer stop/observe guidance.
- Verification:
  - `source /root/miniconda3/etc/profile.d/conda.sh && conda activate openai_glasses && python -m unittest discover -s tests -p 'test_semantic_parked_vehicle_side_policy.py' -v` passed.
  - `source /root/miniconda3/etc/profile.d/conda.sh && conda activate openai_glasses && python -m unittest discover -s tests -p 'test_semantic_*.py' -v` passed.
  - `source /root/miniconda3/etc/profile.d/conda.sh && conda activate openai_glasses && python -m py_compile semantic_output.py tests/test_semantic_parked_vehicle_side_policy.py` passed.

## 2026-02-27 01:29 +0800 - Task #14 completed
- Selected next pending task by AGENT order: `#14 Improve trash-bin/bicycle/e-bike/motorcycle obstacle detection`.
- Root-cause evidence:
  - Sidewalk roadside obstacle classes had weak alias coverage (e.g., `trash can`/`garbage bin`/`e-bike` not normalized to stable canonical labels).
  - In mixed scenes with furniture competition, these target classes could lose priority in Top-K output.
- Implemented task scope (`semantic_output.py`):
  - Added roadside obstacle class constants:
    - `ROADSIDE_OBSTACLE_CLASSES`
    - `ROADSIDE_PRIORITY_SCENES`
  - Added alias normalization:
    - trash bin family (`trash can`/`garbage bin`/`waste bin`/etc -> `trash bin`)
    - e-bike family (`e-bike`/`electric bike`/`ebike` -> `scooter`)
    - related bike aliases.
  - Added roadside score boost policy in `_compute_score(...)`:
    - target classes get extra bonus, further boosted in street/sidewalk/bus-stop/parking-like scenes.
  - Added output safeguard `_force_roadside_obstacle_topk(...)`:
    - in mixed outputs, keeps roadside target classes from being squeezed out by generic furniture.
- Added tests:
  - `tests/test_semantic_roadside_obstacles.py`
    - alias normalization for trash-bin/e-bike targets.
    - target whitelist includes required classes.
    - target classes have higher priority than generic furniture in street scene scoring.
    - output keeps multiple target roadside classes under competition.
- Verification:
  - `source /root/miniconda3/etc/profile.d/conda.sh && conda activate openai_glasses && python -m unittest discover -s tests -p 'test_semantic_roadside_obstacles.py' -v` passed.
  - `source /root/miniconda3/etc/profile.d/conda.sh && conda activate openai_glasses && python -m unittest discover -s tests -p 'test_semantic_*.py' -v` passed (regression).
  - `source /root/miniconda3/etc/profile.d/conda.sh && conda activate openai_glasses && python -m py_compile semantic_output.py tests/test_semantic_roadside_obstacles.py` passed.

## 2026-02-27 01:32 +0800 - Task #15 completed
- Selected next pending task by AGENT order: `#15 Improve low-fence detection`.
- Root-cause evidence:
  - `low fence/short fence/guard rail` aliases were not normalized to canonical `fence`.
  - In mixed roadside scenes, low-height fence cues were often squeezed out by higher-score furniture/vehicle classes.
- Implemented task scope (`semantic_output.py`):
  - Added low-fence alias normalization:
    - `low fence/short fence/guard rail/... -> fence`.
  - Added low-fence geometry policy:
    - `center_y` lower-half and height-ratio gating to identify low-height fence candidates.
  - Added low-fence scoring/risk reinforcement:
    - scene-aware low-fence bonus in `_compute_score(...)`.
    - low-fence risk floor in candidate risk calculation to reduce misses in near-path cases.
  - Added output safeguard `_force_low_fence_topk(...)`:
    - when low fence evidence exists, preserve at least one low-fence cue in final Top-K.
- Added tests:
  - `tests/test_semantic_low_fence_detection.py`
    - low-fence alias normalization to `fence`.
    - low-fence class priority over generic furniture in sidewalk scoring.
    - low-fence cue retained in mixed output and text.
- Verification:
  - `source /root/miniconda3/etc/profile.d/conda.sh && conda activate openai_glasses && python -m unittest discover -s tests -p 'test_semantic_low_fence_detection.py' -v` passed.
  - `source /root/miniconda3/etc/profile.d/conda.sh && conda activate openai_glasses && python -m unittest discover -s tests -p 'test_semantic_*.py' -v` passed (regression).
  - `source /root/miniconda3/etc/profile.d/conda.sh && conda activate openai_glasses && python -m py_compile semantic_output.py tests/test_semantic_low_fence_detection.py` passed.

## 2026-02-27 01:37 +0800 - Task #16 completed
- Selected next pending task by AGENT order: `#16 Add stop-and-gaze (>=2s) focused description mode`.
- Root-cause evidence:
  - Semantic output previously had only dynamic/stable narration branches; no state for sustained stop-and-gaze.
  - Output payloads had no focus-mode flags for downstream behavior/diagnostics.
- Implemented task scope (`semantic_output.py`):
  - Added stop-and-gaze focus state tracking:
    - hold threshold (`focus_gaze_hold_sec`, default 2.0s)
    - yaw stability thresholds (`focus_yaw_rate_max_dps`, `focus_yaw_hold_deg`)
    - low-motion gate (`focus_motion_max`)
    - persistent anchor state (`_focus_anchor_ts`, `_focus_anchor_yaw`, `_focus_active`)
  - Added `_update_focus_mode_state(...)`:
    - triggers focused mode when gaze is stable and low-motion hold exceeds threshold.
    - resets/turns off focused mode on fast turning or unstable conditions.
  - Added focused narration branch in `render_text(...)`:
    - prioritized richer forward description with explicit prefix `重点观察前方`.
  - Added focused metadata into all schema outputs:
    - `focused_mode`
    - `focus_hold_sec`
    - `focus_avg_motion`
- Added tests:
  - `tests/test_semantic_stop_gaze_focus_mode.py`
    - focused mode triggers after >=2s stable stop-and-gaze.
    - turning state disables focused mode.
    - focused mode uses richer forward text.
- Verification:
  - `source /root/miniconda3/etc/profile.d/conda.sh && conda activate openai_glasses && python -m unittest discover -s tests -p 'test_semantic_stop_gaze_focus_mode.py' -v` passed.
  - `source /root/miniconda3/etc/profile.d/conda.sh && conda activate openai_glasses && python -m unittest discover -s tests -p 'test_semantic_*.py' -v` passed (regression).
  - `source /root/miniconda3/etc/profile.d/conda.sh && conda activate openai_glasses && python -m py_compile semantic_output.py tests/test_semantic_stop_gaze_focus_mode.py` passed.

## 2026-02-27 01:41 +0800 - Task #17 completed
- Selected next pending task by AGENT order: `#17 Recognize subway/bus station signs and text`.
- Root-cause evidence:
  - `text_reader.py` dispatch only covered `general/bus/restroom`, missing dedicated station mode.
  - OCR text could show station cues, but no structured station classification (subway/bus) or station-direction message generation.
- Implemented task scope (`text_reader.py`):
  - Added `mode='station'` dispatch in `read_text(...)`.
  - Added `_process_station_results(...)` station parser:
    - recognizes subway/bus station keywords (中英混合),
    - extracts line hints (`2号线` / `Line 3` -> `3号线`) and station-name tokens (`XX站`),
    - detects entry/exit intent and generates direction via `_infer_direction_hint(...)`,
    - outputs station-oriented messages (e.g., `地铁站/公交站 + 方向/出口`).
- Added tests:
  - `tests/test_text_reader_station_mode.py`
    - subway station sign text recognized with station cue.
    - bus station direction is generated.
    - station exit direction is generated.
- Verification:
  - `source /root/miniconda3/etc/profile.d/conda.sh && conda activate openai_glasses && python -m unittest discover -s tests -p 'test_text_reader_station_mode.py' -v` passed.
  - `source /root/miniconda3/etc/profile.d/conda.sh && conda activate openai_glasses && python -m unittest discover -s tests -p 'test_text_reader_*.py' -v` passed (regression).
  - `source /root/miniconda3/etc/profile.d/conda.sh && conda activate openai_glasses && python -m py_compile text_reader.py tests/test_text_reader_station_mode.py` passed.

## 2026-02-27 01:44 +0800 - Task #18 completed
- Selected next pending task by AGENT order: `#18 Improve core model outputs for traffic/crosswalk/blindpath/person/stairs/turnstile`.
- Root-cause evidence:
  - Core navigation entities lacked a unified priority set, so mixed scenes could let key navigation cues be squeezed by non-core classes.
  - `blindpath` aliases were not normalized (`blind path/tactile paving` etc. inconsistent).
  - Crosswalk/traffic-light/blindpath cues had no generic short-memory reinjection path for brief miss frames.
- Implemented task scope (`semantic_output.py`):
  - Added core-nav taxonomy and priority controls:
    - `CORE_NAV_CLASSES`
    - `CORE_NAV_PRIORITY_SCENES`
    - `CORE_NAV_HINT_SCENES`
  - Added blindpath normalization and naming:
    - aliases (`blind path/blind_path/tactile paving/... -> blindpath`)
    - `NAME_ZH` mapping for `blindpath`.
  - Added core-nav score boost in `_compute_score(...)` for traffic/crosswalk/blindpath/person/stairs/turnstile entities.
  - Added core-nav hint memory:
    - `_update_core_nav_hint_cache(...)`
    - `_maybe_inject_core_nav_hints(...)`
    - brief miss reinjection for `crosswalk/traffic light/blindpath/person` under scene/support constraints.
  - Added core-nav output safeguard:
    - `_force_core_nav_topk(...)` keeps core entities in final Top-K when present.
  - Added dedicated guidance actions for:
    - `crosswalk`
    - `traffic light`
    - `blindpath`
- Added tests:
  - `tests/test_semantic_core_nav_consistency.py`
    - core whitelist includes required entities.
    - blindpath aliases normalize correctly.
    - core entities outrank generic furniture in street-scene scoring.
    - crosswalk cue remains stable on brief miss frame via hint memory.
- Verification:
  - `source /root/miniconda3/etc/profile.d/conda.sh && conda activate openai_glasses && python -m unittest discover -s tests -p 'test_semantic_core_nav_consistency.py' -v` passed.
  - `source /root/miniconda3/etc/profile.d/conda.sh && conda activate openai_glasses && python -m unittest discover -s tests -p 'test_semantic_*.py' -v` passed (regression).
  - `source /root/miniconda3/etc/profile.d/conda.sh && conda activate openai_glasses && python -m unittest discover -s tests -p 'test_text_reader_*.py' -v` passed (cross-module regression).
  - `source /root/miniconda3/etc/profile.d/conda.sh && conda activate openai_glasses && python -m py_compile semantic_output.py tests/test_semantic_core_nav_consistency.py text_reader.py tests/test_text_reader_station_mode.py` passed.

## 2026-02-27 01:51 +0800 - Task #19 completed
- Selected next pending task by AGENT order: `#19 Avoidance trigger only for straight-ahead urgent/moving threats`.
- Environment/startup check (after activating `openai_glasses`) and direct run completed:
  - `python app_main.py` launched and created log file `log/app_main-20260227_014758.log`.
  - Environment blockers observed:
    - missing SDK deps: `dashscope`, `openai` (`ModuleNotFoundError` in env import check and startup warning).
    - CUDA compatibility mismatch on RTX 5090 (`sm_120`) with current torch build causing `no kernel image is available for execution on the device`.
- Minimal unblock steps recorded:
  - `source /root/miniconda3/etc/profile.d/conda.sh && conda activate openai_glasses`
  - `pip install dashscope openai`
  - temporary CPU fallback: `AIGLASS_DEVICE=cpu` (or install torch/ultralytics build compatible with `sm_120`).
- Root-cause evidence:
  - `_avoidance()` still produced directional bypass for off-center static obstacles (e.g., `chair` on right yielded `从左侧绕开。`), violating task#19 trigger policy.
- Implemented task scope:
  - `semantic_output.py`
    - added active-avoidance gate: only trigger directional/avoid actions when obstacle is straight-ahead (`12点方向`) or an urgent approaching threat.
    - for off-center non-trigger cases, switched to reminder-style guidance (no `绕开/避让/靠左/靠右` action text).
    - aligned structured output action classification with actual action text (`ActionType` now inferred from stop/avoid tokens instead of urgency-only mapping).
  - tests:
    - added `tests/test_semantic_avoidance_trigger_policy.py` (TDD red->green) for trigger narrowing behavior.
    - updated `tests/test_semantic_far_obstacle_policy.py` near-obstacle assertion to straight-ahead scenario, consistent with task#19 policy.
- Verification:
  - `source /root/miniconda3/etc/profile.d/conda.sh && conda activate openai_glasses && python -m unittest discover -s tests -p 'test_semantic_avoidance_trigger_policy.py' -v` passed (after initial red failure).
  - `source /root/miniconda3/etc/profile.d/conda.sh && conda activate openai_glasses && python -m unittest discover -s tests -p 'test_semantic_*.py' -v` passed.
  - `source /root/miniconda3/etc/profile.d/conda.sh && conda activate openai_glasses && python -m unittest discover -s tests -p 'test_text_reader_*.py' -v` passed (regression).
  - `source /root/miniconda3/etc/profile.d/conda.sh && conda activate openai_glasses && python -m py_compile semantic_output.py tests/test_semantic_avoidance_trigger_policy.py tests/test_semantic_far_obstacle_policy.py` passed.

## 2026-02-27 01:58 +0800 - Task #20 completed
- Selected next pending task by AGENT order: `#20 Suppress elevator hallucination announcements`.
- Environment/startup check with virtual env + direct run completed:
  - `source /root/miniconda3/etc/profile.d/conda.sh && conda activate openai_glasses && python app_main.py`
  - Runtime log created: `log/app_main-20260227_015638.log`.
  - Startup entered normal path; observed existing environment blockers/warnings:
    - missing SDK dependency warning: `No module named 'dashscope'`
    - CUDA mismatch on RTX 5090 (`sm_120`) with current torch build, causing `no kernel image is available for execution on the device` during model warmup.
- Minimal unblock steps recorded:
  - `pip install dashscope openai`
  - temporary CPU fallback `AIGLASS_DEVICE=cpu` (or install torch/ultralytics build compatible with `sm_120`).
- Root-cause evidence:
  - In mixed non-elevator scene probe, a single weak `elevator` candidate still produced elevator narration and output (`names=['elevator', 'table', 'chair']`), i.e. hallucination leak.
- Implemented task scope (`semantic_output.py`):
  - Added elevator evidence gate with configurable thresholds:
    - `AIGLASS_ELEVATOR_MIN_CONF`
    - `AIGLASS_ELEVATOR_MIN_AREA_RATIO`
    - `AIGLASS_ELEVATOR_CONF_WITH_SUPPORT`
    - `AIGLASS_ELEVATOR_SUPPORT_MIN_SCORE`
    - `AIGLASS_ELEVATOR_SUPPORT_MAX_DIST_NORM`
  - Added support scoring for elevator context objects (button/panel/indicator/door-related).
  - Added `_suppress_weak_elevator_candidates(...)` and integrated it before scene inference:
    - suppress isolated weak elevator detections in non-elevator context.
    - retain elevator cue when support evidence is sufficient.
- Added tests:
  - `tests/test_semantic_elevator_hallucination.py`
    - non-elevator mixed scene suppresses weak elevator cue.
    - strong elevator + support evidence keeps elevator cue.
- Verification:
  - `source /root/miniconda3/etc/profile.d/conda.sh && conda activate openai_glasses && python -m unittest discover -s tests -p 'test_semantic_elevator_hallucination.py' -v` passed (after initial red failure).
  - `source /root/miniconda3/etc/profile.d/conda.sh && conda activate openai_glasses && python -m unittest discover -s tests -p 'test_semantic_*.py' -v` passed.
  - `source /root/miniconda3/etc/profile.d/conda.sh && conda activate openai_glasses && python -m unittest discover -s tests -p 'test_text_reader_*.py' -v` passed (cross-module regression).
  - `source /root/miniconda3/etc/profile.d/conda.sh && conda activate openai_glasses && python -m py_compile semantic_output.py tests/test_semantic_elevator_hallucination.py` passed.
