## 0. 目标与范围

### 当前已实现（现状快照）

系统已具备：盲道导航、过马路辅助、物品识别与追踪、实时语音交互、视频WebSocket推流、IMU数据融合与前端可视化等。
整体由 **NavigationMaster** 作为“导航统领层”统一调度盲道/过马路/找物品等模块，并通过 `/ws_ui` 向浏览器推送 `INIT/PARTIAL/FINAL` 消息；前端用 `[AI]`、`[导航]` 前缀区分气泡样式。

### 下一阶段要交付的能力（优先级从高到低）

1. **物品查找增强**：超时“没找到”提示、多目标队列、持续追踪提示、热冷反馈
2. **颜色识别模块**：语音触发“这个是什么颜色/帮我看颜色”，取最新帧输出颜色
3. **文字识别/OCR**：公交车路线牌识别 + 通用汉字识别（建议 OCR）
4. **避障增强 + 头部高度障碍**：危险评估 + 播报节流；头部高度障碍用“单目工程近似”
5. **夜间/较暗区域检测 + 外设LED控制**：亮度阈值进入 NIGHT 模式并下发指令给 ESP32
6. **导航扩展**：路口/台阶/坡道/门/电梯/楼梯识别；结合 IMU 做更稳转向/偏航提示

> “点歌”仍待定，不纳入本阶段硬交付。

------

## 1. 需要遵守的系统约束（Claude 必须按这个集成）

1. **所有新能力必须接入现有状态机/特殊命令体系**：系统已有“开始导航/开始过马路/帮我找一下[物品]”等特殊命令表；新增命令要补齐到同一处（避免散落在多处 if-else）。
2. **UI 输出必须走 /ws_ui 的 FINAL: 消息，且用 [AI] 或 [导航] 前缀**（否则前端气泡分类失效）。
3. **语音输出必须走 audio_player 的 play_voice_text() / play_audio_threadsafe()**（保持现有播放队列与优先级机制）。
4. **不能把 OCR / 颜色识别做成“每帧常驻重计算”**：统一采用“按命令抓取最新帧 + 一次性推理”的交互式模式（和文档描述一致：颜色识别“取最新帧，需要新增模块”）。
5. **尽量复用现有 WebSocket/UDP 接口与数据通路**：
   - `/ws/camera` ESP32→Server 推流
   - `/ws/viewer` Server→Browser 推流
   - `/ws_ui` Server→Browser UI 状态
   - UDP 12345 ESP32 IMU 数据

------

## 2. 任务分解（Claude 的实现清单）

### Epic A：物品查找增强（yolomedia + NavigationMaster）

**背景**：现有已经支持“帮我找一下X”触发 ITEM_SEARCH，且 yolo 侧有模式 SEGMENT/FLASH/CENTER_GUIDE/TRACK。下一阶段按云端部署文档补齐强化点。

#### A1. “没找到”超时与提示

- **需求**：进入 ITEM_SEARCH 后，如果连续 30s（可配置）未检测到目标（或 tracking 丢失超过阈值），播报：
  - `[导航] 我没找到 <目标>，请把镜头换个角度或走近一点。`
- **实现要点**
  - 在 ITEM_SEARCH 会话里维护：`search_start_ts`、`last_seen_ts`、`timeout_count`。
  - 超时后不退出 ITEM_SEARCH，只提示；提示间隔要节流（例如至少 10s 一次）。
  - 配置项：`ITEM_SEARCH_NOT_FOUND_SEC=30`，`ITEM_SEARCH_NOT_FOUND_COOLDOWN_SEC=10`。
- **验收**：对着无目标画面触发“帮我找一下钥匙”，30s 内至少播报一次提示；之后每隔 cooldown 才会重复。

#### A2. 多目标队列（“找钥匙和钱包”）

- **需求**：支持一次命令包含多个目标，按队列依次搜索：
  - 识别“和/以及/还有/并且/，/、”等分隔；生成 `targets=[...]`。
  - 当前目标找到并“抓取成功”或用户说“找到了/拿到了”后切到下一个。
- **UI/语音输出**
  - 切换目标时：`[导航] 现在开始找：<下一个目标>（<i>/<n>）`
- **验收**：命令“帮我找一下钥匙和钱包”能连续执行两轮 ITEM_SEARCH，不需要再次唤起。

#### A3. 目标追踪期间的“方向/距离变化”持续提示

- **需求**：检测到后持续提示目标相对位置与变化趋势（左/中/右 + 远/近）。
- **实现建议**
  - 复用已有“方位描述”逻辑（按 center_x_ratio 三分法）。
  - 距离用 bbox 面积或“物体面积/手面积 ratio”等既有指标（yolomedia 已有 ratio 概念）。
  - 提示节奏：参考已有“播报间隔”设计（不同阶段不同 repeat interval）。
- **验收**：目标在画面从左移动到中间时，语音能从“左侧”更新到“中间”；靠近时提示频率增加。

#### A4. “热冷”反馈（越居中越近 → 频率/音调变化）

- **需求**：在 CENTER_GUIDE / TRACK 阶段提供连续反馈：
  - 越居中：提示更积极、更密集；偏离：提示稀疏并指出方向。
- **实现建议（不改音频引擎的前提）**
  - 只做“播报频率变化 + 文案变化”，不强制做音调变化（除非你已有可变 pitch 的播放链路）。
  - 例：
    - 很准：`[导航] 很好，保持。`（更频繁）
    - 偏左：`[导航] 往右一点。`（较低频）
- **验收**：同一目标在不同偏移程度下，播报间隔明显不同。

------

### Epic B：颜色识别模块（新模块 + 特殊命令）

云端部署明确：收到“这个是什么颜色/帮我看颜色”→取最新帧→新增模块。

#### B1. 新增特殊命令

- 在“特殊命令处理”表中新增两类关键词（与现有表同一位置维护）：
  - “这个是什么颜色 / 帮我看颜色 / 看下颜色 / 颜色识别”
- 触发后：抓取最新 bgr frame，调用 `color_recognition.detect_color(bgr)`，输出 `[AI]` 或 `[导航]` 均可（建议 `[AI] 这是…`）。

#### B2. `color_recognition.py` 设计（一次性推理）

- 输入：`bgr_image (np.ndarray)`
- 输出：`{name_zh, rgb, confidence, method}`
- 默认策略（轻量、鲁棒）：
  1. 取中心 ROI（例如画面中心 30%×30%），避免边缘背景干扰
  2. 转 HSV / Lab
  3. 颜色分类用阈值规则 + 最近邻映射（红/橙/黄/绿/青/蓝/紫/粉/棕/黑/白/灰）
  4. 置信度：ROI 主色占比（可用 kmeans=2 或直方图峰值占比）
- 输出文案：
  - 高置信：`[AI] 我看到主要是：蓝色。`
  - 低置信：`[AI] 可能是：蓝绿色。你可以把镜头再对准一点。`

#### B3. 前端/语音一致性

- 通过 `/ws_ui` 发 `FINAL:`，且内容以 `[AI]` 或 `[导航]` 开头，否则前端不会分类渲染。
- 同时调用 `audio_player.play_voice_text()` 播报（保留统一队列/优先级）。

**验收**：浏览器端出现 AI 气泡 + 语音播报颜色；在导航状态下也能触发，但不应打断“高优先级安全播报”（需要节流/优先级见 Epic D）。

------

### Epic C：OCR/读字（公交车路线牌 + 通用汉字）

云端部署要求“检测 + OCR/读字”，并列出步骤：确定公交车站→读取路线牌→定位车头区域，同时“能识别汉字建议 OCR”。

#### C1. 两个交互命令（都走一次性抓帧）

- **通用读字**：
  - 关键词：“读一下这上面写的什么 / 识别文字 / 读字 / 读一下”
  - 行为：取最新帧 → OCR 全图 → 输出 Top-K 文本（过滤太短/置信度低）
- **公交车路线**：
  - 关键词：“公交车来了是哪一路 / 帮我看公交几路 / 这是几路车”
  - 行为：取最新帧 → 先检测 bus/车头区域（YOLO/YOLO-E 任一）→ 裁剪 route sign ROI → OCR → 输出“XX路/开往XXX”

#### C2. 模块划分

- `ocr_engine.py`：封装 OCR 引擎初始化与推理（建议 PaddleOCR 或等价方案；要求支持中文数字与汉字）
- `text_reader.py`：通用读字逻辑（后处理：合并行、去重、排序）
- `bus_reader.py`：公交路线识别（ROI 策略 + 后处理规则：优先包含“路/线/开往/终点站/起点站”的行）
- 对外统一接口：
  - `read_text_from_frame(bgr, mode="general|bus") -> result`

#### C3. UI 输出规范

- 通用：`[AI] 我读到：……`
- 公交：`[AI] 这辆车可能是：<路线号>。`（如果置信低要提示“可能/不确定”）

**验收**：对一张包含中文文字的画面触发“读字”，前端出现 AI 气泡并播报；对公交车画面触发“公交几路”，能读出路线号（至少输出候选列表）。

------

### Epic D：避障增强 + 头部高度障碍（安全优先）

云端部署要求：危险评估 + 语音节流；头部高度障碍加入“高度判定逻辑（单目工程近似）”。另外你现有系统本身就强调 NavigationMaster 负责“语音节流”。

#### D1. 统一的“安全播报调度器”（强烈建议在 NavigationMaster 内做）

- 目标：避免 OCR/颜色/闲聊抢占“危险提示”。
- 设计：`VoiceScheduler`（或在 navigation_master 里实现）
  - 输入：`(text, priority, cooldown_key, min_interval)`
  - 行为：同类消息按 cooldown 节流；高优先级可打断低优先级排队
- 优先级建议：
  - P0：紧急避障/头部障碍
  - P1：导航转向/对齐
  - P2：物品查找引导
  - P3：AI 闲聊 / OCR / 颜色

#### D2. 危险评估（risk scoring）

- 输入：障碍物检测结果（bbox + class + 与盲道/行进方向关系）
- 输出：risk_level={LOW/MED/HIGH} + 方位（左/前/右）+ 距离近似（用 bbox 面积/底部位置）
- 文案示例：
  - HIGH：`[导航] 前方近距离障碍，请停下。`
  - MED：`[导航] 左侧有人靠近。`（文档里给了类似例子）

#### D3. 头部高度障碍（单目工程近似）

- 不追求真实高度估计，先做“风险分类”：
  - **ground obstacle**：bbox 底边接近画面底部（更像地面障碍）
  - **head-level obstacle**：bbox 底边不靠近底部 + bbox 位于画面中上部 + bbox 面积/宽度达到一定阈值（表示“可能接近且在上方”）
- 输出专用文案：`[导航] 注意头部高度有障碍。`

**验收**：模拟检测到近距离大 bbox 时，播报频率不会刷屏；头部类条件满足时能输出“注意头部高度…”，并且优先级高于 OCR/颜色。

------

### Epic E：夜间/较暗检测 + LED 外设控制（发指令给 ESP32）

需求明确：每 N 帧均值亮度 < 阈值 → NIGHT 模式 → 发指令给 ESP32；并控制 LED。

#### E1. 夜间检测器（常驻轻量）

- `night_mode.py`：维护滑动窗口亮度均值（例如每 10 帧采样一次）
- 参数：
  - `NIGHT_SAMPLE_EVERY_N_FRAMES`（默认 10）
  - `NIGHT_LUMA_THRESH`（默认如 40/255）
  - `NIGHT_HYSTERESIS`（退出阈值更高，避免抖动）
  - `NIGHT_STABLE_FRAMES`（连续满足才切换）
- 切换事件：
  - 进入：`on_night_mode(True)`
  - 退出：`on_night_mode(False)`

#### E2. 下发 ESP32 指令的通道（新增 WS，避免破坏现有 /ws/camera）

现有接口里 `/ws/camera` 标注为 ESP32→Server，为了不影响现有推流，建议新增：

- **WS /ws_cmd（ESP32 ↔ Server）**
  - ESP32 建立连接后常驻等待命令
  - Server 发送 JSON：`{"type":"LED","mode":"BLINK","pattern":"SOS"}` 或 `{"type":"NIGHT","on":true}`
  - ESP32 回 ACK：`{"ok":true,"type":"LED"}`（可选）

#### E3. 行为策略

- 进入 NIGHT：LED 进入“慢闪”或“SOS”（由你们硬件侧决定）
- 退出 NIGHT：LED 关闭
- UI：`[导航] 已进入夜间提示模式。` / `已退出夜间模式。`

**验收**：暗光环境持续 2-3 秒后进入 NIGHT，亮起来后退出；Server 能在日志看到 `/ws_cmd` 成功发送命令。

------

### Epic F：导航扩展（路口/台阶/坡道/门/电梯/楼梯）+ IMU 稳定提示

需求：新增检测模型即可 + 结合 IMU 做更稳“转向/偏航提示”。

#### F1. 场景检测（可插拔）

- `scene_detector.py`：加载一个 YOLO 检测模型（若暂时没有模型，可先留接口 + mock 输出）
- NavigationMaster 在 BLINDPATH_NAV/CROSSING 状态下按固定间隔调用（例如每 8-12 帧一次），识别到关键类则播报：
  - 台阶：`[导航] 前方可能有台阶。`
  - 楼梯：`[导航] 前方可能是楼梯，请小心。`
  - 电梯/门：更偏“提示信息”，优先级低于避障

#### F2. IMU 辅助转向确认（不改变现有 UDP 接收）

既有 UDP 12345 接收 IMU JSON。

- 新增 `imu_state.py`：从 gyro 积分得到粗略 yaw（无需严格姿态融合，先做“转向是否发生”的判定）
- 用于两类地方：
  1. 盲道 onboarding 的“旋转对准”子步骤更稳（已有 onboarding/turn 子步骤定义）
  2. 转弯口播后，确认用户确实转向了再进入下一步（降低误触）

**验收**：发出“向左转”后，若 IMU 没检测到旋转，隔一段时间再提醒一次；检测到旋转后不再重复催促。

------

## 3. 需要同步更新的“指令词表”（统一入口）

现有特殊命令表包含导航/过马路/红绿灯/找物品等。本阶段要求把下面新增进去（同一处维护）：

- 颜色：这个是什么颜色｜帮我看颜色｜颜色识别
- OCR：读字｜识别文字｜读一下这上面写的什么
- 公交：公交车来了是哪一路｜这是几路车｜帮我看公交几路
- 夜间（可选手动开关）：打开夜间模式｜关闭夜间模式（用于覆盖自动检测）

------

## 4. 联调与验收清单（最小可交付）

1. **颜色识别**：任意画面触发→UI 出现 `[AI]` 气泡且播报颜色
2. **物品查找增强**：
   - 30s 找不到会提示
   - “找钥匙和钱包”队列依次执行
3. **OCR**：触发“读字/公交几路”能出结果（允许低置信提示“可能”）
4. **避障与头部障碍**：危险播报不刷屏，且优先级压过 OCR/颜色
5. **夜间模式**：暗光触发→server 通过新 `/ws_cmd` 下发 LED 指令

