# 系统优化计划：语音播报修复 + 数据传输移除 + 户外提醒 + 音频路由 + 主动场景识别

## 修改目标

1. **修复语音播报问题**：排查并修复有画面无语音的问题
2. **移除数据传输占用**：注释掉视频/音频存储和事件记录代码
3. **户外天黑提醒**：检测到户外天黑时提醒用户开灯让别人知道是盲人
4. **麦克风耳机二选一**：连接耳机后不再外音播报，动态切换音频输出
5. **主动场景识别**：接收到画面后自动运行功能并主动输出给用户

---

## 问题1：有摄像头画面但没有语音播报

### 可能原因分析

根据代码探索，语音不工作的可能原因：

1. **导航状态未启动**：orchestrator 处于 IDLE/CHAT 状态，未进入盲道导航模式
2. **音频映射缺失**：`AUDIO_MAP` 中没有对应语音文件的路径
3. **音频系统未初始化**：`initialize_audio_system()` 失败
4. **TTS 未启用且预录音频缺失**：无音频文件可播放

### 修复方案

#### 1.1 添加启动时测试语音

在 `app_main.py` 启动时播放测试语音，验证音频系统：

```python
@app.on_event("startup")
async def startup_event():
    # ... 现有初始化代码 ...
    initialize_audio_system()

    # 【新增】播放测试语音
    time.sleep(1)  # 等待音频系统初始化
    play_voice_text("系统已启动，语音正常")
```

#### 1.2 检查音频映射

确保 `audio_player.py` 中的 `AUDIO_MAP` 和 `voice/map.zh-CN.json` 包含常用语音：

```python
AUDIO_MAP = {
    "系统已启动": "music/converted_系统已启动.wav",
    "向前": "music/converted_向前.wav",
    "向左": "music/converted_向左.wav",
    "向右": "music/converted_向右.wav",
    "前方有障碍物": "music/converted_前方有障碍物.wav",
    # ...
}
```

#### 1.3 添加调试日志

```python
def play_voice_text(text: str):
    print(f"[AUDIO] 尝试播放: {text}")
    # ... 原有逻辑 ...
    if audio_file and os.path.exists(audio_file):
        print(f"[AUDIO] 找到音频文件: {audio_file}")
    else:
        print(f"[AUDIO] 未找到音频，TTS可用: {_tts_enabled}")
```

### 修改文件

| 文件 | 操作 |
|------|------|
| `audio_player.py` | 添加调试日志 |
| `app_main.py` | 启动时播放测试语音 |

---

## 问题2：移除数据传输占用（视频/音频存储）

### 需要注释的代码

| 文件 | 行数/位置 | 操作 |
|------|----------|------|
| `app_main.py` | 314-317行 | 注释 sync_recorder 启动 |
| `app_main.py` | 1608-1613行 | 注释 `sync_recorder.record_frame()` |
| `app_main.py` | 2100-2111行 | 注释 event_logger 初始化 |
| `app_main.py` | 2155-2167行 | 注释 event_logger 关闭 |
| `app_main.py` | 1685-1688行 | 注释 semantic event 日志 |
| `audio_stream.py` | 81-86行 | 注释音频录制调用 |

**注意**：保留 `sync_recorder.py` 和 `event_logger.py` 文件，只注释调用

### 保留功能

**保留** WebSocket 的 `/ws/viewer` 和 `/ws/camera`（需要看画面）

### 具体修改

#### app_main.py

```python
# 第314-317行：注释掉录制器启动
# recorder = sync_recorder.get_recorder()
# recorder.start_recording(...)
# atexit.register(lambda: recorder.stop_recording())

# 第1608-1613行：注释掉帧录制
# try:
#     sync_recorder.record_frame(data)
# except Exception as e:
#     ...

# 第2100-2111行：注释掉事件记录器
# import event_logger
# event_logger = event_logger.EventLogger(...)
# event_logger.start()

# 第2155-2167行：注释掉关闭
# if event_logger:
#     event_logger.stop()

# 第1685-1688行：注释掉语义事件日志
# if event_logger is not None:
#     event_logger.log({...})
```

#### audio_stream.py

```python
# 第81-86行：注释掉音频录制
# try:
#     import sync_recorder
#     sync_recorder.record_audio(pcm16, text="[Omni对话]")
# except Exception:
#     pass
```

---

## 问题3：户外天黑提醒开灯

### 设计思路

结合现有的 `night_mode.py`（夜间检测），增加户外场景判断：

1. **检测条件**：
   - 夜间模式已激活（亮度 < 阈值）
   - 场景推断为"户外"（基于 `semantic_output.py` 的场景判断）
   - 用户正在导航中（非静止）

2. **提醒策略**：
   - 只提醒一次（带冷却时间，如10分钟）
   - 语音内容："天色已晚，建议打开指示灯，让别人注意到您。"

3. **实现位置**：

在 `app_main.py` 的帧处理循环中添加：

```python
# 夜间模式检测后，判断是否需要提醒开灯
night_detector = get_night_detector()
night_light_reminder_cooldown = 600  # 10分钟冷却
last_night_light_remind_time = 0

if night_detector is not None and bgr is not None:
    result = night_detector.process_frame(bgr)

    # 户外天黑提醒
    if result.get('is_night', False):
        current_time = time.time()
        if (current_time - last_night_light_remind_time) > night_light_reminder_cooldown:
            # 检查是否户外（可用场景推断或简单判断）
            is_outdoor = _check_if_outdoor(bgr)  # 新增辅助函数

            if is_outdoor:
                msg = "天色已晚，建议打开指示灯，让别人注意到您。"
                play_voice_text(msg)
                await ui_broadcast_final(f"[导航] {msg}")
                last_night_light_remind_time = current_time
```

### 辅助函数：判断是否户外

```python
def _check_if_outdoor(bgr_image) -> bool:
    """简单判断是否在户外环境"""
    # 方法1：基于亮度分布（户外天空较亮）
    gray = cv2.cvtColor(bgr_image, cv2.COLOR_BGR2GRAY)
    h, w = gray.shape

    # 上半部分（天空）和下半部分亮度对比
    top_half = gray[:h//2, :]
    bottom_half = gray[h//2:, :]
    top_mean = np.mean(top_half)
    bottom_mean = np.mean(bottom_half)

    # 户外特征：天空比地面亮，且整体不太暗
    return (top_mean > bottom_mean * 1.2) and (top_mean > 30)
```

### 修改文件

| 文件 | 操作 |
|------|------|
| `app_main.py` | 添加户外天黑提醒逻辑 |
| `night_mode.py` | 可选：添加户外判断方法 |

---

## 问题4：麦克风耳机二选一（音频动态路由）

### 设计思路

1. **检测蓝牙连接状态**：定时检查蓝牙耳机是否连接
2. **动态切换输出**：
   - 蓝牙连接 → 音频输出到蓝牙（不外音）
   - 蓝牙断开 → 音频输出到本地扬声器
3. **麦克风输入独立**：使用 `ws_audio` 接收ESP32麦克风输入，与输出分离

### 实现方案

#### 4.1 增强蓝牙连接检测

修改 `bluetooth_audio.py`，添加定时检测：

```python
class BluetoothAudioManager:
    def __init__(self):
        self.connected = False
        self.last_check_time = 0
        self.check_interval = 5  # 每5秒检查一次

    def check_connection(self) -> bool:
        """检查蓝牙连接状态"""
        try:
            result = subprocess.run(
                ["pactl", "list", "sinks", "short"],
                capture_output=True, text=True
            )
            # 检查是否有bluez设备连接
            self.connected = "bluez" in result.stdout.lower() and "RUNNING" in result.stdout
            return self.connected
        except Exception:
            return False
```

#### 4.2 修改 audio_player.py 动态路由

```python
def _play_audio_with_routing(audio_path: str):
    """根据蓝牙连接状态动态选择输出"""
    # 检查当前蓝牙状态
    using_bluetooth = False
    if _bluetooth_manager:
        using_bluetooth = _bluetooth_manager.check_connection()

    if using_bluetooth:
        # 使用PulseAudio默认sink（应该是蓝牙）
        _play_via_pulse(audio_path)
    else:
        # 本地播放（pygame或其他）
        _play_local(audio_path)
```

#### 4.3 ESP32 麦克风输入独立处理

麦克风输入通过 `/ws_audio` 接收，与音频输出是独立的 WebSocket 连接，无需修改。

### 修改文件

| 文件 | 操作 |
|------|------|
| `bluetooth_audio.py` | 添加 `check_connection()` 方法 |
| `audio_player.py` | 修改播放函数支持动态路由 |

---

## 问题5：主动场景识别与播报

### 当前问题

系统目前需要用户说"开始导航"才会进入导航模式并播报信息。用户希望：
1. **接收到画面后自动运行检测**
2. **主动播报有用信息**

### 设计思路

在 `app_main.py` 的帧处理循环中，添加**自动场景识别**逻辑：

```
接收到画面
    ↓
【自动场景检测】（每帧或每隔几帧）
    ├─ 检测到盲道 → 主动播报 "前方有盲道"
    ├─ 检测到斑马线 → 主动播报 "发现斑马线"
    ├─ 检测到障碍物 → 主动播报 "前方有障碍物"
    ├─ 检测到红绿灯 → 主动播报 "前方是红灯"
    └─ 未知场景 → 周期性描述环境
```

### 实现方案

#### 5.1 启用自动场景检测模式

在 `app_main.py` 添加新的自动模式变量：

```python
# 自动场景识别（默认开启）
auto_scene_detection = os.getenv("AIGLASS_AUTO_SCENE_DETECTION", "1") == "1"
auto_detection_interval = 3.0  # 每3秒检测一次（避免频繁播报）
last_auto_detection_time = 0

# 当前检测到的场景（用于平滑切换）
current_detected_scene = "unknown"  # blindpath / crosswalk / obstacle / traffic_light / unknown
scene_confidence_frames = 0  # 连续检测帧数
```

#### 5.2 在帧处理循环中添加自动检测

```python
# 在 ws_camera_esp 的帧处理中，orchestrator 处理之后
if auto_scene_detection and bgr is not None:
    current_time = time.time()

    # 按间隔检测（避免过于频繁）
    if (current_time - last_auto_detection_time) >= auto_detection_interval:
        last_auto_detection_time = current_time

        # 【自动场景检测】
        scene, confidence = _detect_scene(bgr)

        # 场景切换或高置信度时播报
        if scene != current_detected_scene and confidence > 0.7:
            current_detected_scene = scene
            msg = _get_scene_announcement(scene)
            if msg:
                play_voice_text(msg)
                await ui_broadcast_final(f"[导航] {msg}")
```

#### 5.3 场景检测函数

```python
def _detect_scene(bgr: np.ndarray) -> Tuple[str, float]:
    """检测当前场景，返回 (scene_type, confidence)"""
    # 1. 检测盲道（使用 workflow_blindpath）
    try:
        from workflow_blindpath import BlindPathNavigator
        blind_nav = BlindPathNavigator()
        blind_result = blind_nav.process_frame(bgr)
        if blind_result.has_blind_path and blind_result.confidence > 0.7:
            return "blindpath", blind_result.confidence
    except Exception:
        pass

    # 2. 检测斑马线（使用 workflow_crossstreet）
    try:
        from workflow_crossstreet import CrossStreetNavigator
        cross_nav = CrossStreetNavigator()
        cross_result = cross_nav.detect_crosswalk(bgr)
        if cross_result.has_crosswalk and cross_result.confidence > 0.7:
            return "crosswalk", cross_result.confidence
    except Exception:
        pass

    # 3. 检测红绿灯
    try:
        from navigation_master import TrafficLightDetector
        detector = TrafficLightDetector()
        color, _ = detector._try_backend(bgr)
        if color in ["red", "green", "yellow"]:
            return f"traffic_light_{color}", 0.8
    except Exception:
        pass

    # 4. 检测障碍物（使用 obstacle_detector）
    try:
        if obstacle_detector is not None:
            detections = obstacle_detector.detect(bgr)
            # 找最近的障碍物
            if detections:
                closest = max(detections, key=lambda d: d.get('area', 0))
                if closest.get('area', 0) > 10000:  # 较大面积的障碍物
                    return "obstacle", 0.7
    except Exception:
        pass

    return "unknown", 0.0
```

#### 5.4 场景播报文本

```python
def _get_scene_announcement(scene: str) -> str:
    """根据场景返回播报文本"""
    announcements = {
        "blindpath": "前方检测到盲道",
        "crosswalk": "发现斑马线",
        "traffic_light_red": "前方是红灯",
        "traffic_light_green": "前方是绿灯",
        "traffic_light_yellow": "前方是黄灯",
        "obstacle": "前方有障碍物，注意安全",
        "unknown": None  # 未知场景不播报
    }
    return announcements.get(scene)
```

### 修改文件

| 文件 | 操作 |
|------|------|
| `app_main.py` | 添加自动场景检测逻辑 |
| `.env.example` | 添加 `AIGLASS_AUTO_SCENE_DETECTION` 配置 |

### 配置选项

```bash
# 自动场景识别
AIGLASS_AUTO_SCENE_DETECTION=1     # 启用自动场景识别
AIGLASS_AUTO_DETECTION_INTERVAL=3  # 检测间隔（秒）
```

---

## 实施步骤（按顺序）

### Step 1：修复语音播报
1. 在 `audio_player.py` 添加调试日志
2. 在 `app_main.py` 启动时播放测试语音
3. 运行测试，检查日志输出

### Step 2：移除数据传输占用
1. 注释 `app_main.py` 中的录制相关代码
2. 注释 `audio_stream.py` 中的音频录制
3. 可选：删除/重命名 `sync_recorder.py` 和 `event_logger.py`

### Step 3：户外天黑提醒
1. 在 `app_main.py` 添加夜间检测后的户外判断
2. 实现提醒逻辑（带冷却时间）
3. 测试验证

### Step 4：音频动态路由
1. 增强 `bluetooth_audio.py` 的连接检测
2. 修改 `audio_player.py` 的播放路由逻辑
3. 测试蓝牙连接/断开时的音频切换

### Step 5：主动场景识别
1. 在 `app_main.py` 添加自动场景检测逻辑
2. 实现场景检测函数（盲道、斑马线、红绿灯、障碍物）
3. 实现场景播报函数
4. 测试验证不同场景下的播报

---

## 验证测试

### 测试1：语音播报
- 启动系统后能听到"系统已启动"
- 进入导航模式后有语音提示
- 检查日志确认音频文件路径正确

### 测试2：数据传输已移除
- `recordings/` 目录不再生成新文件
- 帧率保持在25fps左右
- 内存占用降低

### 测试3：户外天黑提醒
- 夜间户外环境下触发提醒
- 提醒后10分钟内不再重复
- 日志输出 `[NIGHT_LIGHT]` 相关信息

### 测试4：音频路由
- 蓝牙连接时音频从耳机输出
- 蓝牙断开时音频从扬声器输出
- 麦克风输入正常工作

### 测试5：主动场景识别
- 启动系统后自动检测场景
- 检测到盲道时播报 "前方检测到盲道"
- 检测到斑马线时播报 "发现斑马线"
- 检测到障碍物时播报 "前方有障碍物"
- 检测到红绿灯时播报颜色
- 播报间隔合理（约3秒一次）

---

## 关键文件清单

| 文件 | 修改类型 | 说明 |
|------|----------|------|
| `audio_player.py` | 修改 | 添加日志、动态路由 |
| `app_main.py` | 修改 | 测试语音、注释录制、户外提醒、主动场景识别 |
| `audio_stream.py` | 修改 | 注释音频录制 |
| `bluetooth_audio.py` | 修改 | 增强连接检测 |

---

## 配置文件更新

在 `.env.example` 添加：

```bash
# 户外天黑提醒
AIGLASS_NIGHT_LIGHT_REMINDER=1      # 启用户外天黑提醒
AIGLASS_NIGHT_LIGHT_COOLDOWN=600    # 提醒冷却时间（秒）

# 音频动态路由
AIGLASS_AUDIO_AUTO_SWITCH=1         # 自动切换音频输出
AIGLASS_AUDIO_CHECK_INTERVAL=5      # 蓝牙检测间隔（秒）

# 自动场景识别
AIGLASS_AUTO_SCENE_DETECTION=1      # 启用自动场景识别
AIGLASS_AUTO_DETECTION_INTERVAL=3   # 检测间隔（秒）
```

---

## 风险与注意事项

1. **语音文件缺失**：如果 `music/` 目录下没有对应 wav 文件，需要确保 TTS 可用
2. **蓝牙检测延迟**：`pactl` 命令调用可能有延迟，需要测试实际效果
3. **户外判断准确性**：简单的亮度分布判断可能误判，可根据实际情况调整
4. **Night light 提醒时机**：避免在导航关键播报时打断
5. **主动识别计算开销**：每帧运行多个检测器可能影响帧率，已设置3秒间隔控制频率
6. **场景误报**：自动识别可能误判场景，需设置合理的置信度阈值

---

## 回滚方案

如果某项修改有问题：
1. 语音播报：检查日志，确认是音频文件问题还是播放器问题
2. 数据传输：取消注释即可恢复录制功能
3. 户外提醒：设置 `AIGLASS_NIGHT_LIGHT_REMINDER=0` 关闭
4. 音频路由：设置 `AIGLASS_AUDIO_OUTPUT=local` 强制本地输出
5. 主动场景识别：设置 `AIGLASS_AUTO_SCENE_DETECTION=0` 关闭自动识别
