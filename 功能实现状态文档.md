# AI智能助盲眼镜 - 功能实现状态文档

> 更新时间: 2026-01-21
> 项目路径: /data0/home/scli/Codes/OpenAIglasses_for_Navigation-main

---

## 功能清单总览

| 功能 | 实现状态 | 实现文件 | 触发方式 |
|------|----------|----------|----------|
| 1. 寻找丢失的物品 | ✅ 已实现 | yolomedia.py, item_search_enhancer.py | "帮我找xxx" |
| 2. 颜色识别 | ✅ 已实现 | color_recognition.py | "这个是什么颜色" |
| 3. 寻找食物/实物 | ✅ 已实现 | yolomedia.py (YOLOE文本提示) | "帮我找xxx" |
| 4. 智能导航 | ✅ 已实现 | navigation_master.py, workflow_blindpath.py | "开始导航" |
| 5. 点歌 | ✅ 已实现 | music_controller.py | "我想听xxx" |
| 6. 及时避障 | ✅ 已实现 | obstacle_detector_client.py, voice_scheduler.py | 自动检测 |
| 7. 公交车路线识别 | ✅ 已实现 | text_reader.py (BusReader) | "这是几路车" |
| 8. 识别朋友/人脸分析 | ✅ 已实现 | face_friend_recognition.py, app_main.py | "这是谁" / "这是张三" |
| 9. 汉字识别(OCR) | ✅ 已实现 | text_reader.py (OCREngine) | "读一下这上面写的什么" |
| 10. 头部高度障碍检测 | ✅ 已实现 | voice_scheduler.py (RiskAssessor) | 自动检测 |
| 11. 夜间盲人信号提示 | ✅ 已实现 | night_mode.py, ESP32指令 | 自动检测 |
| 12. 灯光关闭提醒 | ✅ 已实现 | light_reminder.py, app_main.py | "灯关了吗" / "提醒我关灯" |

**实现率: 12/12 (100%)**

---

## 详细功能说明

### 1. 寻找丢失的物品 ✅

**实现文件**: `yolomedia.py`, `item_search_enhancer.py`

**核心功能**:
- 使用 YOLOE 文本提示分割模型进行物品检测
- 光流追踪技术锁定目标
- 多目标队列支持（"找钥匙和钱包"）
- 超时"没找到"提示（默认30秒）
- 持续方向/距离引导（热冷反馈：越居中频率越高）

**触发方式**:
```
"帮我找钥匙"           # 单目标
"找一下钥匙和钱包"     # 多目标队列
"识别一下杯子"         # 通用物品识别
```

**实现特点**:
- 自动锁定：检测到物体后1秒自动锁定
- 闪烁动画：锁定时黄色闪烁确认
- 居中引导：语音+画面引导物品移到中心
- 手部追踪：MediaPipe手骨架检测��引导抓取

---

### 2. 颜色识别 ✅

**实现文件**: `color_recognition.py`

**核心功能**:
- HSV色彩空间进行初步分类
- Lab色彩空间进行精确匹配
- 支持12种颜色：红、橙、黄、绿、青、蓝、紫、粉、棕、黑、白、灰

**触发方式**:
```
"这个是什么颜色"
"帮我看颜色"
"看下颜色"
```

**实现原理**:
1. 提取画面中心区域ROI
2. HSV颜色空间初步判断色相范围
3. Lab颜色空间计算与标准色的欧氏距离
4. 返回中文名称+RGB值+置信度

---

### 3. 寻找食物/实物 ✅

**实现文件**: `yolomedia.py` (使用YOLOE文本提示后端)

**核心功能**:
- YOLOE支持任意文本提示的物体检测
- 不需要预先训练类别
- 支持食物、日用品等各类物品

**触发方式**:
```
"帮我找苹果"
"找一下水杯"
"识别一下遥控器"
```

**模型**: YOLOE分割模型 (位于 `model/shoppingbest5.pt` 或 YOLOE后端)

---

### 4. 智能导航 ✅

**实现文件**: `navigation_master.py`, `workflow_blindpath.py`, `workflow_crossstreet.py`, `crosswalk_awareness.py`

**导航状态机**:
```
IDLE → CHAT → BLINDPATH_NAV → SEEKING_CROSSWALK → WAIT_TRAFFIC_LIGHT → CROSSING → SEEKING_NEXT_BLINDPATH → BLINDPATH_NAV
```

**核心功能**:
- **盲道导航**: 检测盲道并给出方向引导
- **斑马线检测**: 面积阈值判断斑马线阶段
- **红绿灯识别**: HSV颜色判定 + 多数表决滤波
- **过马路引导**: 检测对侧斑马线和盲道
- **恢复模式**: 感知丢失时的兜底处理

**触发方式**:
```
"开始导航"     # 启动盲道导航
"停止导航"     # 停止导航
"开始过马路"   # 进入过马路状态
"立即通过"     # 强制通过
```

---

### 5. 点歌 ✅

**实现文件**: `music_controller.py`, `app_main.py`

**核心功能**:
- 多源音乐搜索（咪咕/网易云/酷狗）
- 语音控制搜索（"我想听xxx"）
- 播放列表管理（下一首、上一首、播放指定编号）
- 本地下载（网易云/酷狗可直链下载）
- ESP32播放指令发送

**触发方式**:
```
"我想听青花瓷"         # 搜索并显示结果
"来一首周杰伦的晴"     # 搜索
"播放第1首"           # 播放指定编号
"下一首" / "上一首"    # 切歌
"暂停" / "继续播放"    # 播放控制
```

**实现方式**:
- 咪咕搜索API: `https://pd.musicapp.migu.cn/MIGUM3.0/v1.0/content/search_all.do`
- 网易云搜索API: `https://music.163.com/api/search/get/web`
- 酷狗搜索API: `http://msearchcdn.kugou.com/api/v3/search/song`
- 统一输出 `SongInfo`（歌名/歌手/URL/封面/ID/大小/来源）
- 下载接口：`download_song(song, save_path)` 直接写入本地文件
  - 网易云：`api/song/enhance/player/url/v1` 获取直链
  - 酷狗：`trackercdn.kugou.com/i/v2` 获取直链
- 通过WebSocket发送ESP32播放指令

**ESP32指令格式**:
```json
{"type": "MUSIC", "action": "search_result", "songs": [...]}
{"type": "MUSIC", "action": "play", "url": "...", "name": "..."}
{"type": "MUSIC", "action": "pause"}
```

**下载验证**:
- 网易云成功下载：`music_downloads/刘芳 - 青花瓷.mp3`
- 酷狗成功下载：`music_downloads/周杰伦 - 青花瓷 (2012CCTV-15音乐频道精彩音乐汇现场).mp3`

**限制**:
- 咪咕：resourceinfo 未返回可用直链（当前无法下载，仅能用网页链接）
- 酷狗：付费曲目无法拿到直链（仅能下载 `pay_type=0` 的免费版本）
- 网易云：搜索结果可能出现同名/合唱版本，需要进一步过滤（如仅匹配“周杰伦”）
- ESP32端需要实现HTTP音频流播放功能

---

### 6. 及时避障 ✅

**实现文件**: `obstacle_detector_client.py`, `voice_scheduler.py`, `voice_scheduler.py:RiskAssessor`

**核心功能**:
- YOLOE白名单障碍物检测
- 风险等级评估 (LOW/MEDIUM/HIGH)
- 头部高度障碍特殊检测
- 优先级队列调度 (P0紧急 > P1导航 > P2物品搜索 > P3闲聊)

**检测类别**:
```python
'bicycle', 'car', 'motorcycle', 'bus', 'truck', 'animal', 'scooter', 'stroller', 'dog',
'pole', 'post', 'column', 'pillar', 'stanchion', 'bollard', 'utility pole',
'bench', 'chair', 'potted plant', 'hydrant', 'cone', 'stone', 'box'
```

**风险阈值**:
- 近距离障碍: 面积比 ≥ 15%
- 危险距离: 面积比 ≥ 25%
- 头部高度: 底部距画面底部 ≥ 20% 且中心在画面中上部

---

### 7. 公交车路线识别 ✅

**实现文件**: `text_reader.py` (BusReader类)

**核心功能**:
- OCR识别公交车路牌
- 关键词过滤（"路"、"线"、"开往"等）
- 数字提取和格式化

**触发方式**:
```
"公交车来了是哪一路"
"这是几路车"
"公交车是几路"
```

**OCR引擎优先级**:
1. PaddleOCR (推荐，支持中文)
2. EasyOCR
3. Tesseract

---

### 8. 识别朋友/人脸分析 ✅

**实现文件**: `face_friend_recognition.py`, `app_main.py`

**核心功能**:
- 本地人脸检测：Haar Cascade
- 本地人脸识别：OpenCV LBPH（离线优先）
- 支持朋友档案：姓名 +（可选）性别/年龄
- 本地落盘人脸库：`context/faces/`（`db.json` + `images/*.png` + `lbph.yml`）

**触发方式**:
```
"这是谁" / "谁在我面前"         # 识别
"这是张三"                      # 录入
"这是张三，男，30岁"            # 录入（含属性）
"朋友列表" / "我认识谁"          # 列表
"忘记张三" / "删除张三"          # 删除
```

**说明/限制**:
- 性别/年龄目前以“录入时语音提供的元数据”为主（不做强行猜测），避免误导。

---

### 9. 汉字识别(OCR) ✅

**实现文件**: `text_reader.py` (OCREngine + TextReader)

**核心功能**:
- 通用文字识别（中文+英文）
- 去重和位置排序
- 置信度过滤

**触发方式**:
```
"读一下这上面写的什么"
"识别文字"
"读字"
"这是什么字"
```

**OCR引擎优先级**:
1. PaddleOCR
2. EasyOCR
3. Tesseract

---

### 10. 头部高度障碍检测 ✅

**实现文件**: `voice_scheduler.py` (RiskAssessor类, `_is_head_level_obstacle`方法)

**核心功能**:
- 判断障碍物是否在头部高度
- 判断条件:
  - 底部不靠近画面底部（距底部≥20%）
  - 中心在画面中上部（Y < 0.6）
  - 面积足够大（> 3%）

**特殊处理**: 头部高度障碍会提高风险等级（LOW→MEDIUM）

---

### 11. 夜间盲人信号提示 ✅

**实现文件**: `night_mode.py`, `app_main.py` (ESP32指令)

**核心功能**:
- 亮度采样检测（默认每60帧）
- 迟滞机制防止频繁切换（开阈值/关阈值分离）
- ESP32 LED控制指令

**亮度阈值**:
- 日间模式阈值: 80 (默认)
- 夜间模式阈值: 60 (默认)
- 迟滞: 夜间→日间需要更高亮度

**ESP32指令格式**:
```json
{"type": "NIGHT", "on": true, "mode": "LOW_BEACON"}
```

**手动控制**:
```
"打开夜间模式"
"关闭夜间模式"
```

---

### 12. 灯光关闭提醒 ✅

**实现文件**: `light_reminder.py`, `app_main.py`

**核心功能**:
- 一次性检查：根据“局部高亮区域 + 整体亮度”启发式判断灯是否可能仍开启
- 常驻提醒（可选）：用户开启后，仅在 `CHAT/IDLE` 模式下周期性检测并节流提醒

**触发方式**:
```
"灯关了吗" / "检查灯有没有关"     # 一次性检查
"提醒我关灯" / "开启关灯提醒"     # 开启常驻提醒
"关闭关灯提醒" / "停止关灯提醒"   # 关闭
```

**说明/限制**:
- 该功能为启发式估计，强光/反光/户外环境可能造成误判，可通过环境变量调阈值。

---

## 系统架构

### 核心模块依赖

```
app_main.py (FastAPI主程序)
├── yolomedia.py              # YOLO检测 + 物品追踪
├── navigation_master.py      # 导航状态机
├── workflow_blindpath.py     # 盲道导航
├── workflow_crossstreet.py   # 过马路
├── obstacle_detector_client.py  # 障碍物检测
├── voice_scheduler.py        # 语音调度 + 风险评估
├── color_recognition.py      # 颜色识别
├── text_reader.py            # OCR文字识别
├── night_mode.py             # 夜间模式
├── item_search_enhancer.py   # 物品搜索增强
├── crosswalk_awareness.py    # 斑马线感知
├── yoloe_backend.py          # YOLOE后端
├── qwen_extractor.py         # Qwen AI提取
├── omni_client.py            # Omni API客户端
└── bridge_io.py              # 视频桥接
```

### WebSocket端点

| 端点 | 功能 |
|------|------|
| `/ws/ui` | UI界面通信 |
| `/ws/audio` | 音频流输入 |
| `/ws/camera` | ESP32摄像头输入 |
| `/ws/viewer` | 画面观看 |
| `/ws/imu` | IMU传感器数据 |
| `/ws/cmd` | ESP32指令输出 |

---

## 可选升级建议（已实现后续可迭代）

### 8. 人脸识别（可选升级）

**可选升级方案**:
```python
# face_recognition.py (新建)
from insightface.app import FaceAnalysis

class FaceRecognizer:
    def __init__(self):
        self.model = FaceAnalysis(providers=['CUDAExecutionProvider'])
        self.model.prepare(ctx_id=0, det_size=(640, 640))
        self.known_faces = {}  # 存储已知人脸

    def recognize(self, image):
        faces = self.model.get(image)
        results = []
        for face in faces:
            name, similarity = self.match(face.embedding)
            gender = self.get_gender(face)
            age = self.get_age(face)
            results.append({
                'name': name,
                'similarity': similarity,
                'gender': gender,
                'age': age
            })
        return results
```

**可能新增依赖**: insightface, onnxruntime-gpu

### 12. 灯光关闭提醒（可选升级）

**可选升级方案**:
```python
# light_detector.py (新建)
class LightDetector:
    def detect_lights(self, image):
        # 检测画面中的光源（高亮度区域）
        # 判断是否为室内灯光
        # 返回灯光状态
        pass

    def remind_before_leave(self):
        # 离家前检测灯光状态
        # 如有开启则提醒
        pass
```

---

## 配置参数

### 环境变量

| 变量 | 默认值 | 说明 |
|------|--------|------|
| AIGLASS_DEVICE | cuda:0 | GPU设备 |
| AIGLASS_AMP | bf16 | 自动混合精度策略 |
| AIGLASS_GPU_SLOTS | 2 | GPU并发限制 |
| AIGLASS_OBS_CONF | 0.25 | 障碍物检测置信度 |

### 重要阈值

| 参数 | 默认值 | 位置 |
|------|--------|------|
| 物品搜索超时 | 30秒 | item_search_enhancer.py |
| 语音引导间隔 | 1.5秒 | yolomedia.py |
| 夜间检测间隔 | 60帧 | night_mode.py |
| 红绿灯多数表决 | 8帧 | navigation_master.py |
| 风险评估面积阈值 | 15%/25% | voice_scheduler.py |

---

## 已知问题与限制

1. **PaddleOCR依赖**: 需要安装 paddlepaddle-gpu 和 paddleocr，可能存在版本兼容问题
2. **GPU内存**: 多个模型同时加载可能占用大量显存
3. **实时性**: OCR处理较慢，可能影响帧率
4. **人脸识别**: 未实现，需要额外模型
5. **点歌功能**: 需要第三方音乐API

---

## 开发建议

### 优先实现功能（按重要性）

1. **人脸识别/朋友识别** - 高价值功能
2. **点歌功能** - 提升用户体验
3. **灯光关闭提醒** - 生活辅助

### 性能优化

1. 使用模型量化减小内存占用
2. GPU并发限制防止OOM
3. 语音播报节流避免频繁打断

### 测试建议

1. 各功能独立测试
2. 长时间运行稳定性测试
3. 不同光照条件下测试
